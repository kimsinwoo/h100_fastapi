# ==============================
# Z-Image AI Service (Z-Image-Turbo only)
# Python 3.11+
# ==============================

# --- API Layer ---
fastapi>=0.109.0,<0.115.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9

# --- ML (Z-Image-Turbo: diffusers에서 ZImageImg2ImgPipeline) ---
# torch와 torchvision은 반드시 같은 PyTorch 릴리스로 맞출 것 (버전 불일치 시 torchvision::nms 오류)
torch>=2.2.0
torchvision>=0.17.0
diffusers>=0.36.0
transformers>=4.36.0
accelerate>=0.26.0
safetensors>=0.4.0
Pillow>=10.0.0

# 한국어 LoRA 파인튜닝 등 (선택)
datasets>=2.16.0
peft>=0.10.0
trl>=0.8.0